import { describe, expect, it } from 'vitest';
import { QUESTIONS } from './questions';
import { ANSWERS, emptyStats, heartsAfter, hitResult, launchVelocity, pullLimit, scoreHit, shuffleTargets, validTransition } from './logic';
describe('question bank',()=>{it('has valid unique, explained answers',()=>{expect(QUESTIONS.length).toBeGreaterThanOrEqual(60);expect(new Set(QUESTIONS.map(q=>q.id)).size).toBe(QUESTIONS.length);for(const q of QUESTIONS){expect(ANSWERS).toContain(q.answer);expect(q.explanation.length).toBeGreaterThan(3);expect(q.sentence).toContain('___');}});});
describe('game logic',()=>{it('shuffles without duplicates',()=>{const a=shuffleTargets(['am','is','are'],()=>.01);expect(new Set(a).size).toBe(3);expect(a.sort()).toEqual(['am','are','is']);});it('limits pull and calculates opposite launch',()=>{const p=pullLimit(300,0);expect(p.d).toBe(180);expect(launchVelocity(p).x).toBeLessThan(0);});it('scores correct hits and evaluates answers',()=>{expect(scoreHit(true,1000,1,true)).toBeGreaterThan(100);expect(scoreHit(false,0,0)).toBe(0);expect(hitResult(QUESTIONS[0],'am').correct).toBe(true);expect(hitResult(QUESTIONS[0],'is').correct).toBe(false);});it('uses hearts only in challenge and spends shield',()=>{expect(heartsAfter('practice',3,false).hearts).toBe(3);expect(heartsAfter('challenge',3,false).hearts).toBe(2);expect(heartsAfter('challenge',3,false,true)).toEqual({hearts:3,shield:false});expect(emptyStats('challenge').hearts).toBe(3);});it('allows defined state transitions',()=>{expect(validTransition('aiming','projectileFlight')).toBe(true);expect(validTransition('results','projectileFlight')).toBe(false);});});
